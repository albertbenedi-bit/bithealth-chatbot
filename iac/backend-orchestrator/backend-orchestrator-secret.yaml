apiVersion: v1
kind: Secret
metadata:
  name: backend-orchestrator-secrets
  namespace: general-chatbot-app
type: Opaque
data:
  GEMINI_API_KEY: ""  # Base64 encoded value to be set during deployment
  ANTHROPIC_API_KEY: ""  # Base64 encoded value to be set during deployment
